## 2024-05-23 - [Grid Accessibility Upgrade]
**Learning:** Converting `div` based grids to `button` elements instantly enables keyboard navigation (tabbing) without complex `tabindex` management.
**Action:** Always prefer semantic `<button>` elements for interactive grid items, even if they look like simple shapes. Use `aria-pressed` for toggle states.
